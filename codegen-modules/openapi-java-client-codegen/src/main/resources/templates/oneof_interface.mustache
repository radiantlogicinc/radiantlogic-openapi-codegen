{{>additionalOneOfTypeAnnotations}}{{>generatedAnnotation}}{{>typeInfoAnnotation}}{{>xmlAnnotation}}
{{#discriminator}}
{{^hasDiscriminatorWithNonEmptyMapping}}
// NOTE: The lack of a discriminator mapping means serialization/deserialization of this interface and its implementations can only be done with the Raw inner class.
{{/hasDiscriminatorWithNonEmptyMapping}}
{{/discriminator}}
public interface {{classname}} {{#vendorExtensions.x-implements}}{{#-first}}extends {{{.}}}{{/-first}}{{^-first}}, {{{.}}}{{/-first}}{{/vendorExtensions.x-implements}} {
    {{#discriminator}}
    {{propertyType}} {{propertyGetter}}();

    {{^hasDiscriminatorWithNonEmptyMapping}}
    /**
     * This represents the raw JSON data associated with this type. If this has been included
     * by the codegen in this interface, that means the original OpenAPI spec was incomplete,
     * lacking necessary discriminator mapping information to properly de-serialize into the
     * appropriate implementation of this interface.
     *
     * This Raw type can be manually de-serialized into the appropriate implementation by
     * calling code as-necessary.
     */
    class Raw extends HashMap<String, Object> {
        public {{propertyType}} {{propertyGetter}}() {
            return ({{propertyType}}) get("{{propertyName}}");
        }
    }
    {{/hasDiscriminatorWithNonEmptyMapping}}
    {{/discriminator}}

{{#vars}}
    {{#isEnum}}
        {{^isContainer}}
{{>modelInnerEnum}}
        {{/isContainer}}
        {{#isContainer}}
            {{#mostInnerItems}}
{{>modelInnerEnum}}
            {{/mostInnerItems}}
        {{/isContainer}}
    {{/isEnum}}
{{/vars}}
}
